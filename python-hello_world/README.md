# Python Hello World

This directory contains shell scripts and Python code demonstrating how to execute Python scripts from the command line using environment variables.

## 0-run

This task involves writing a shell script that runs a Python script. The Python script's file name is stored in the `PYFILE` environment variable. Here's an example of how to use it:

```bash
export PYFILE=main.py
./0-run
